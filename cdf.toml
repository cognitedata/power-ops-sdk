[cdf]
default_env = "dev"
default_organization_dir = "toolkit"

[alpha_flags]
require-kind = true
edges = true

[modules]
# This is the version of the modules. It should not be changed manually.
# It will be updated by the 'poetry run cdf-tk modules upgrade' command.
version = "0.5.75"


[plugins]
run = true
pull = false
dump = false
