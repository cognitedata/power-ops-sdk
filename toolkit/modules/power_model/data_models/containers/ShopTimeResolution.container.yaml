space: "{{power_ops_models_space}}"
externalId: ShopTimeResolution
name: ShopTimeResolution
usedFor: node
properties:
  name:
    type:
      list: false
      collation: ucs_basic
      type: text
    nullable: false
    autoIncrement: false
    name: name
    description: Name of the SHOP time resolution.
  minutesAfterStart:
    type:
      list: true
      type: int64
    nullable: false
    autoIncrement: false
    name: minutesAfterStart
    description: Minutes after SHOP Simulation start.
  timeResolutionMinutes:
    type:
      list: true
      type: int64
    nullable: false
    autoIncrement: false
    name: timeResolutionMinutes
    description: The SHOP time resolution (in minutes) to use for SHOP.
