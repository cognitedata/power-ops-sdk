space: '{{powerops_models}}'
externalId: PriceScenario
name: PriceScenario
usedFor: node
properties:
# The following is repeated in Mapping - should this be remodeled?
  timeseries:
    type:
      list: false
      type: timeseries
    nullable: false
    autoIncrement: false
    name: timeseries
    description: The time series to apply mapping to
  retrieve:
    type:
      list: false
      collation: ucs_basic
      type: text
    nullable: false
    autoIncrement: false
    name: retrieve
    description: How to retrieve time series data
  aggregation:
    type:
      list: false
      collation: ucs_basic
      type: text
    nullable: false
    autoIncrement: false
    name: aggregation
    description: How to aggregate time series data
  transformations:
    type:
      list: true
      type: json
    nullable: true
    autoIncrement: false
    name: transformations
    description: The transformations to apply to the time series
indexes:
  timeSeriesIndex:
    properties:
    - timeseries
    indexType: btree
    cursorable: false
