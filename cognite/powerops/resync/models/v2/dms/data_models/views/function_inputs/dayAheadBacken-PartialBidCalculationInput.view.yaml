space: '{{day_ahead_backend_space}}'
externalId: PartialBidCalculationInput
description: Input to the task dispatcher in the shop bid process
name: PartialBidCalculationInput
version: '{{day_ahead_backend_partial_bid_calculation_input_version}}'
implements:
  - space: '{{day_ahead_backend_space}}'
    externalId: FunctionInput
    version: '{{day_ahead_backend_function_input_version}}'
    type: view
filter:
  equals:
    property:
    - node
    - property
    - functionName
    value: PartialBidCalculation
properties:
  plant_name:
    container:
      space: '{{day_ahead_backend_space}}'
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: text1
    name: plant_name
    description: The name of the plant to calculate the partial bid for
  shopResults:




plant: String # from bidconfig that task dispatcher receives
shop_results: [SHOPresult] #results from each shop scenario where the plant is included
process_id: String
previous_step: TaskDispatcherOutput
process_step: Int
alerts: [Alert]