space: '{{powerops_models}}'
externalId: Scenario
description: SHOP scenario that holds all data needed for a shop run. May be unprocessed or processed
name: Scenario
version: '{{version}}'
implements:
  - space: '{{powerops_models}}'
    externalId: ScenarioBase
    version: '{{version}}'
    type: view
properties: {}
filter:
  equals:
    property:
    - {{powerops_models}}
    - ScenarioBase/{{version}}
    - isReady
    value: False
