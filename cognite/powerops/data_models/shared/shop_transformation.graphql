type ShopTransformation {
    # CDF does store the type, thus a type with only one to many edges
    # will display all nodes in the entire space. This is a workaround
    type_name: String
    start: [DateTransformation]
    end: [DateTransformation]
}
