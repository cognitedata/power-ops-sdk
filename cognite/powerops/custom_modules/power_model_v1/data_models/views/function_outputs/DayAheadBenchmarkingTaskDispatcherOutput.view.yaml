space: "{{power_ops_models}}"
externalId: DayAheadBenchmarkingTaskDispatcherOutput
name: DayAheadBenchmarkingTaskDispatcherOutput
description: The task dispatcher output data for benchmarking
filter:
  and:
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
    - equals:
        property:
          - node
          - type
        value:
          externalId: DayAheadBenchmarkingTaskDispatcherOutput
          space: "{{power_ops_type_space}}"
implements:
  - space: "{{power_ops_models}}"
    externalId: FunctionOutput
    version: "{{version}}"
    type: view
version: "{{version}}"
properties:
  input: # Overrides the view type from the FunctionOutput view
    container:
      space: "{{power_ops_models}}"
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: linkedStep
    name: input
    source:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingTaskDispatcherInput
      version: "{{version}}"
      type: view
  benchmarkingShopRunInputs:
    type:
      space: "{{power_ops_type_space}}"
      externalId: benchmarkingShopRunInputs
    source:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingShopRunInput
      version: "{{version}}"
      type: view
    direction: outwards
    name: benchmarkingShopRunInputs
    description: An array of benchmarking shop run inputs.
    connectionType: multi_edge_connection
