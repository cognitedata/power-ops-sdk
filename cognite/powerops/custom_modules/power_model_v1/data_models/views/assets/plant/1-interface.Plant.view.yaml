space: "{{power_ops_models}}"
externalId: Plant
name: Plant
description: TODO interface for typing
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: Plant
implements:
  - space: "{{power_ops_models}}"
    externalId: PowerAsset
    version: "{{version}}"
    type: view
version: "{{version}}"
properties: {}
