space: "{{power_ops_models}}"
externalId: BidMatrixSimple
name: BidMatrixSimple
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: BidMatrix
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
implements: []
version: "{{version}}"
properties:
  name:
    container:
      space: "{{power_ops_models}}"
      externalId: BidMatrix
      type: container
    containerPropertyIdentifier: name
    name: name
  bidMatrix:
    container:
      space: "{{power_ops_models}}"
      externalId: BidMatrix
      type: container
    containerPropertyIdentifier: bidMatrix
    name: bidMatrix
