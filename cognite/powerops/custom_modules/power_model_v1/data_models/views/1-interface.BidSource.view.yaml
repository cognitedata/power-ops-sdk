space: "{{power_ops_models}}"
externalId: BidSource
name: BidSource
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: FunctionData
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
implements: []
version: "{{version}}"
properties:
  timeseries:
    type:
      list: false
      type: timeseries
    nullable: true
    autoIncrement: false
    name: timeseries
    description: TODO
  name:
    type:
      list: false
      collation: ucs_basic
      type: text
    nullable: false
    autoIncrement: false
    name: name
    description: The name of the bid source
