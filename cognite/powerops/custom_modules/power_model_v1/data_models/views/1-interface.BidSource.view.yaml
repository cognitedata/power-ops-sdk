space: "{{power_ops_models}}"
externalId: BidSource
name: BidSource
description: TODO
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: FunctionData
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
implements: []
version: "{{version}}"
properties:
  timeseries:
    container:
      type: container
      space: '{{power_ops_models}}'
      externalId: FunctionData
    containerPropertyIdentifier: timeSeries1
    name: timeseries
    description: TODO
  name:
    container:
      type: container
      space: '{{power_ops_models}}'
      externalId: FunctionData
    containerPropertyIdentifier: text1
    name: name
    description: The name of the bid source
