space: "{{power_ops_models}}"
externalId: DayAheadBenchmarkingShopRunInput
name: DayAheadBenchmarkingShopRunInput
description: Class for inputs to shop runs used for day ahead benchmarking
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: DayAheadBenchmarkingShopRunInput
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
    - equals:
        property:
          - node
          - type
        value:
          externalId: DayAheadBenchmarkingShopRunInput
          space: "{{power_ops_type_space}}"
implements:
  - space: "{{power_ops_models}}"
    externalId: FunctionInput
    version: "{{version}}"
    type: view
version: "{{version}}"
properties:
  case:
    container:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingShopRunInput
      type: container
    containerPropertyIdentifier: case
    name: case
    description: "The SHOP case (with all details like model, scenario, and time series)"
    source:
      space: "{{power_ops_models}}"
      externalId: ShopCase
      version: "{{version}}"
      type: view
  bidSource: # A BidConfiguration, or DayAheadProductionObligationForBenchmarking, or a simple instance of BidSource for "upper bound"
    container:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingShopRunInput
      type: container
    containerPropertyIdentifier: bidSource
    name: bidSource
    source:
      space: "{{power_ops_models}}"
      externalId: BidSource
      version: "{{version}}"
      type: view
  deliveryDate:
    container:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingShopRunInput
      type: container
    containerPropertyIdentifier: deliveryDate
    name: deliveryDate
    description: The delivery date
  bidGenerated:
    container:
      space: "{{power_ops_models}}"
      externalId: DayAheadBenchmarkingShopRunInput
      type: container
    containerPropertyIdentifier: bidGenerated
    name: bidGenerated
    description: Timestamp of when the bid had been generated
