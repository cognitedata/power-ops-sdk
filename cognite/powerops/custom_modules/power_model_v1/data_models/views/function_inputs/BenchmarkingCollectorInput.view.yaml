space: "{{power_ops_models}}"
externalId: BenchmarkingCollectorInput
name: BenchmarkingCollectorInput
description: The benchmarking collector/ input data.
filter:
  and:
    - hasData:
        - type: container
          space: "{{power_ops_models}}"
          externalId: FunctionData
    - equals:
        property:
          - node
          - space
        value: "{{power_ops_instance_space}}"
    - equals:
        property:
          - node
          - type
        value:
          externalId: BenchmarkingCollectorInput
          space: "{{power_ops_type_space}}"
implements:
  - space: "{{power_ops_models}}"
    externalId: FunctionInput
    version: "{{version}}"
    type: view
version: "{{version}}"
properties:
  shopResults:
    type:
      space: "{{power_ops_type_space}}"
      externalId: ShopResults
    source:
      space: "{{power_ops_models}}"
      externalId: ShopResult
      version: "{{version}}"
      type: view
    direction: outwards
    name: shopResults
    description: An array of shop results.
    connectionType: multi_edge_connection
  shopRunInputs:
    type:
      space: "{{power_ops_type_space}}"
      externalId: ShopRunInputs
    source:
      space: "{{power_ops_models}}"
      externalId: BenchmarkingShopRunInputDayAhead
      version: "{{version}}"
      type: view
    direction: outwards
    name: shopRunInputs
    description: An array of benchmarking shop run inputs.
    connectionType: multi_edge_connection
