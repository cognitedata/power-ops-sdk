space: "{{powerops_models}}"
externalId: PartialBidCalculationInput
name: PartialBidCalculation
description: The data for a bid calculation for water value based method
filter:
  and:
    - hasData:
        - type: container
          space: "{{powerops_models}}"
          externalId: FunctionData
    - equals:
        property:
          - node
          - space
        value: "{{powerops_instance_space}}"
implements:
  - space: "{{powerops_models}}"
    externalId: FunctionInput
    version: "{{version}}"
    type: view
version: "{{version}}"
properties:
  bidDate:
    container:
      space: "{{powerops_models}}"
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: date1
    name: bidDate
    description: The bid date
  marketConfiguration:
    container:
      space: "{{powerops_models}}"
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: direct1
    name: priceArea
    description: The market configuration related to the bid calculation task
    source:
      space: "{{powerops_models}}"
      externalId: MarketConfiguration
      version: "{{version}}"
      type: view
  priceArea:
    container:
      space: "{{powerops_models}}"
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: direct2
    name: priceArea
    description: The price area related to the bid calculation task
    source:
      space: "{{powerops_models}}"
      externalId: PriceArea
      version: "{{version}}"
      type: view
