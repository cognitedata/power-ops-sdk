space: '{{powerops_models}}'
externalId: ShopPartialBidCalculationInput
name: ShopPartialBidCalculationInput
description: Input to the task dispatcher in the shop bid process
filter:
  and:
  - hasData:
    - type: container
      space: '{{powerops_models}}'
      externalId: FunctionData
  - equals:
      property:
      - node
      - space
      value: '{{powerops_instance_space}}'
  - equals:
      property:
      - node
      - type
      value:
        externalId: ShopPartialBidCalculationInput
        space: '{{powerops_type_space}}'
implements:
- space: '{{powerops_models}}'
  externalId: FunctionInput
  version: '{{version}}'
  type: view
version: '{{version}}'
properties:
  plant:
    container:
      space: '{{powerops_models}}'
      externalId: FunctionData
      type: container
    containerPropertyIdentifier: direct1
    name: plant
    description: The plant to calculate the partial bid for
    source:
      space: '{{powerops_models}}'
      externalId: PlantShop
      version: '{{version}}'
      type: view
  shopResults:
    type:
      space: '{{powerops_type_space}}'
      externalId: SHOPResult
    source:
      space: '{{powerops_models}}'
      externalId: SHOPResult
      version: '{{version}}'
      type: view
    direction: outwards
    name: shopResults
    description: An array of shop results.
    connectionType: multi_edge_connection
  alerts:
    type:
      space: '{{powerops_type_space}}'
      externalId: calculationIssue
    source:
      space: '{{powerops_models}}'
      externalId: Alert
      version: '{{version}}'
      type: view
    direction: outwards
    name: alerts
    description: An array of calculation level Alerts.
    connectionType: multi_edge_connection
